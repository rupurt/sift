# Runnable Embedded Example CLI - Decision Log

<!-- Append entries below. Each entry is an H2 with ISO timestamp. -->
<!-- Use `keel mission digest` to compress older entries when this file grows large. -->

## 2026-03-10T16:54:34

Completed the runnable embedded example slice: examples/sift-embed provides a standalone facade-only consumer, the repo root now exposes embed-build/embed-search recipes, and docs point to sift-embed as the canonical runnable embedding reference. Repo-wide just check remains blocked by the known mold/glibc linker failure in this environment.
