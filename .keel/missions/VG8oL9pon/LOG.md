# Structural Retrieval Documentation Fidelity - Decision Log

<!-- Append entries below. Each entry is an H2 with ISO timestamp. -->
<!-- Use `keel mission digest` to compress older entries when this file grows large. -->

## 2026-04-07T14:01:00-07:00

- Created mission `VG8oL9pon` and epic `VG8oL9Xok` to raise foundational-document fidelity for the shipped structural retrieval substrate.
- Scoped the work to documentation and board artifacts only, with explicit attention to strategy guidance and the downstream `paddles` direct-search boundary.

## 2026-04-07T14:09:00-07:00

- Refreshed the foundational document set so the structural retrieval stack is described consistently across the conceptual, operator, embedder, and release references.
- Added explicit strategy-selection guidance for `path-fuzzy`, `segment-fuzzy`, `path-hybrid`, and `page-index-hybrid`, and documented the `paddles` seam as richer direct retrieval through explicit `SearchPlan` helpers rather than planner ownership transfer.
- Verified doc coverage with targeted `rg` checks and confirmed the structural plan helpers remain exposed with focused cargo tests.

## 2026-04-07T14:05:47

Mission achieved by local system user 'alex'
