# Reflection - Add Homebrew Installer Support

We have successfully integrated Homebrew formula generation into our automated release pipeline. By adding `homebrew` to the `cargo-dist` installers and configuring a dedicated tap (`rupurt/homebrew-tap`), we've made it significantly easier for macOS and Linux users to install and update `sift`. The documentation has been updated to reflect this new installation method.
